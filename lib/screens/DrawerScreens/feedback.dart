import 'package:flutter/material.dart';

class FeedbackReport extends StatelessWidget {
  const FeedbackReport({Key? key}) : super(key: key);
  static String id = 'feedback';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
