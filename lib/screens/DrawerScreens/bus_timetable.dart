import 'package:flutter/material.dart';

class BusTimetable extends StatelessWidget {
  const BusTimetable({Key? key}) : super(key: key);
  static String id = 'busTime';

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('text'),
      ),
    );
  }
}
